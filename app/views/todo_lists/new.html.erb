<h1>New todo list</h1>

<%= form_for(@todo_list) do |f| %>
    <div class="title ">
      <%= f.label :title %>
      <%= f.text_field(:title) %>
    </div>
  <div class="submit">
    <%= f.submit 'Save' %>
  </div>
<% end %>

